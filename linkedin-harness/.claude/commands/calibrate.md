---
description: Log real post performance, mine what worked, update the voice profile and swipe file
---

Calibrate the harness against real results. Input (post + numbers, or blank to review): $ARGUMENTS

1. Ask the user for performance of recently published posts (impressions, reactions, comments, saves/sends if visible, profile views, follows). Record the numbers into the post's frontmatter in `posts/published/` (move it from drafts/ if needed).
2. Once ≥5 published posts have numbers, mine the corpus: which hooks, formulas, formats, topics, and lengths over- and under-performed *for this account*. Account-level data beats global benchmarks — treat these findings as overrides.
3. Write findings into `voice/swipe-file.md` (winners, with the why) and, when a pattern is solid (≥3 posts), update the voice profile's formula/goal preferences and note the override in CLAUDE.md's operating rules ONLY if the user agrees.
4. Also check the misses: posts that died in the golden window (was the hook pre-fold weak? was it a link/bridge post? off-topic for the account's authority cluster?). One-line diagnosis each.
5. Summarize: 3 things to do more, 2 things to stop, and the single highest-leverage change for next week.
