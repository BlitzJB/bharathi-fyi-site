---
description: Turn an article, README, talk, changelog, or tweet into native LinkedIn post(s)
---

Repurpose this source into LinkedIn post(s): $ARGUMENTS

1. Ingest the source (file, URL via WebFetch, or pasted text). Save raw material worth keeping to `research/`.
2. Use the vendored `linkedin-repurposer` skill's approach: extract the **content atoms** (Corey Haines taxonomy) — quotable moments, story arcs, tactical tips, controversial takes, data callouts. One atom = one post; never compress the whole source into one post.
3. For each atom worth shipping (show the user the list first, let them pick): run the `/draft` pipeline from step 4 onward (goal → formula → draft → humanize → critic). Long tactical sources often want a document/carousel (1.39x reach) — if so, also outline the slides (one of the 5 carousel architectures: Value-Stack, Problem-Proof, Hack List, Rant Callout, Demo Walkthrough).
4. Native means native: no "I wrote a blog post about X, link below" framing. The post must deliver standalone value; the source link goes to first comment or gets edited in later, per CLAUDE.md link rules.
