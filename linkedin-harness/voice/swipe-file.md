# Swipe file

Winners — the account's own top posts and admired posts from others — each with a note on WHY it worked (hook pattern, formula, format, specificity, topic fit). `/voice-setup` and `/calibrate` append here; `/hooks` and the post-critic read it. Account-level evidence in this file overrides global benchmarks.

<!-- entries below -->

## 2026-09-06 — Netflix LLM-judge lifecycle (draft, user-approved pre-publish)

`posts/drafts/2026-09-06-netflix-llm-judge-lifecycle.md`. First post through the pipeline; user verdict: "content is great, generate more like this."

Why it worked (per the user's own feedback loop):
- The v1 failed on comprehension: it assumed the reader knew what "LLM judge quality-gating recommendation explanations" meant. The fix that made it work: explain the system from zero, quote the paper's real example blurb verbatim, then make the point.
- Explicit one-line takeaway near the close ("your judge's reasoning is part of the pipeline, not a debug log") — the quotable line doubles as the diagram's caption.
- E1-curious register: tension in the field -> counterintuitive move (right verdict, wrong reason) -> open question. No forced personal angle.
- Authority moves: institution named in the hook AND "in this paper from Netflix" attribution early; square screenshot of the paper page attached alongside the redrawn mechanism diagram.
- Hook: 9 words, named entity + implied number ("two ways. One made it worse.").
