# Voice samples

Drop 5–10 of the account's REAL posts (or emails/talks) here, one file each, ideally named with rough performance: `2026-08-14-cache-post-12k-imp.md`. This corpus is what the humanizer calibrates against — a writing sample takes priority over style rules — and what `/voice-setup` analyzes to build the voice fingerprint.
